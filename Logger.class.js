/**
 * Created by Giacomo on 30/01/2017.
 *
 * Dummy Logger class.
 *
 */
 'use strict'

class Logger{

    constructor(){ }

    config( /* json object */) {};

    log( /* arbitrary number of parameters */ ){};

    info( /* arbitrary number of parameters */ ){};

    warn( /* arbitrary number of parameters */ ){};

    error( /* arbitrary number of parameters */ ){};
};

module.exports = Logger;
